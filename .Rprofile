# Prefer pak for renv's package manager if available
options(renv.config.pak.enabled = TRUE)
Sys.setenv(RENV_CONFIG_PAK_ENABLED = "TRUE")

if (file.exists("renv/activate.R")) {
  source("renv/activate.R")
}
